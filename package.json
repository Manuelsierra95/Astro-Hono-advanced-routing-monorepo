{
  "name": "astro-hono-advanced-routing-monorepo",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --filter './apps/*' dev",
    "build": "bun run --filter './apps/*' build",
    "start": "bun run --filter './apps/*' start"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "hono": "^4.12.27"
    }
  }
}
